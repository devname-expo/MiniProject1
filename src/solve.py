# Solve for 1 number and an upper bound
def solve_for_one(i: int, limit: int) -> int:
    # if limit is not divisible by i
    ## return limit
    # else
    ## return limit - 1

    return None


# Solve for 2 coprimes and an upper bound
def solve_for_coprimes(l: list, limit: int) -> int:

    return None


# Solve for 2+ numbers and an upper bound
def solve_for_many(l: list, limit: int) -> int:

    return None
